#include "child.h"

void* CHILD_THREAD::run(void* param_data)
{
	/* to search unup record. */
	TOOLSFUNCTION toolsfunc;
	unsigned int i,j;

 	uint32 nowtime = nowtimestamp();
    std::string now_time = anytostr(nowtime);	

    return NULL;
}
